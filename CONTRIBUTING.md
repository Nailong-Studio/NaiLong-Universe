# 贡献指南 · 奶蛙艺术圣殿

感谢你愿意为奶蛙圣殿添砖加瓦。无论你提交素材、写叙事、修官网还是补文档，都请先读完这篇指南。

## 圣殿守则（硬性）

- **奶蛙 ≠ 奶龙**：素材与文案中只出现奶蛙（民间 AI 变异体：黄圆润 / 浅米腹 / 绿眼 / 灰爪）。官方奶龙素材需标注来源并尊重版权。
- **命名统一**：`分类_主题_序号` 中文命名，前缀一律「奶蛙-」。不要用拼音缩写、不要用英文命名、不要用无意义的 `IMG_0001`。
- **每件素材必须带叙事**：同名 `.txt`，与素材同目录，内容是一段可读的叙事（参考现有作品：一句场景 + 一句情绪）。
- **不重复入库**：提交前先在素材仓搜一遍关键词，与现有素材重复的（像素级对照）会被拒收。

## 我能贡献什么

### 1. 提交奶蛙素材（最常见）

- 目标目录：
  - `classic/` 名画再创作（奶蛙主演的古典名画）
  - `laugh-gallery/` 大笑藏馆（大笑奶蛙变体）
  - `emotes/` 表情（日常斗图表情）
  - `art/` 艺术场景（AI 艺术图 / 场景脑洞）
  - `hd/` 高清壁纸
  - `videos/` 官方圣影视频
- 命名：`奶蛙-弯腰大笑.png` / `奶蛙-表情-01.jpeg`
- 配套 txt：与素材同名，写清叙事
- 提交 PR 到 `Nailong-Studio/wallpaper`

### 2. 为官网写叙事 / 改 bug

- 官网是**自包含单文件** `docs/index.html`（CSS/JS 内联，无构建步骤）
- 本地直接双击打开即可预览；改完用浏览器 DevTools 看 console 无报错
- 多「页面」用 hash 路由：`#home` `#halls` `#chronicle` `#visit`
- 新作品插入对应展厅的 `hall__track`，**必须**包含 `data-title` / `data-narrative` / `data-hall` / `data-original`
- 资源放 `docs/assets/`，路径写相对路径 `assets/xxx`，只写 `src` 或 CSS `url()`（发布链只扫这两处）

### 3. 文档 / 主题 / 调色板

- `palette.json` 是调色板唯一真源，改色只改这一个文件
- `themes/` 下的主题按各自说明维护

## PR 流程

1. Fork 本仓库 → 新建分支 `feat/你的改动` 或 `fix/你的改动`
2. 提交改动，commit message 用中文一句话说清（如 `feat: 大笑藏馆新增奶蛙-雀跃`）
3. 开 PR 到 `main`，描述里写：改了什么、为什么、是否引入新素材（素材须已在 wallpaper 仓入库）
4. 通过审查后合并；合并后 GitHub Actions 自动部署官网

## 行为准则

参与本项目即代表同意 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)：对所有人友善、尊重版权、不灌水、不搬运无关内容。
